# WebSocket Server Configuration
PORT=8080
AUTH_TOKEN=your-secure-token-here-change-this-in-production

# For production, generate a secure token:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
